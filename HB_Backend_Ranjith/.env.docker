# Docker Environment Configuration
# Copy this file to .env.docker.local and update with your values

# Application
NODE_ENV=production
DOMAIN=localhost

# PostgreSQL Configuration
POSTGRES_USER=ajith
POSTGRES_PASSWORD=
POSTGRES_DB=hb
# Use 'trust' auth method since no password is set (NOT recommended for production)
POSTGRES_HOST_AUTH_METHOD=trust

# Typesense Configuration
TYPESENSE_API_KEY=Hh873bbdS8044w1291

# Production Example:
# NODE_ENV=production
# DOMAIN=api.hotbray.com
# POSTGRES_USER=hb_production_user
# POSTGRES_PASSWORD=secure_random_password_here
# POSTGRES_DB=hb_production
# TYPESENSE_API_KEY=production_api_key_here
